---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class:list={["option-card-list", className]}>
  <slot />
</div>

<style>
  .option-card-list {
    display: grid;
    gap: var(--spacing-gap-md);
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .option-card-list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
</style>
